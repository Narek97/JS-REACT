{{!--morachat paroli verakangnum minjev emailov hastately --}}

<div class="row">

    <div id="login" class="col s6 offset-s3">
        {{!-- stugum enq ete error ka chuych enq talis --}}
        {{#if error}}
        <p class="alert">{{error}}</p>
        {{/if}}

        <h3>Forgot your password?</h3>
        <form action="/auth/reset" method="POST">
            <div class="input-field">
                <input id="email" name="email" type="email" class="validate" required>
                <label for="email">Email</label>
                <span class="helper-text" data-error="Add email"></span>
            </div>

            {{!-- _csrf sesian pashtpanelu hamar --}}
            <input type="hidden" name="_csrf" value="{{csrf}}">
            {{!-- ............................... --}}
            <button class='btn btn-primary' type="submit">Reset the password</button>

        </form>
    </div>

</div>